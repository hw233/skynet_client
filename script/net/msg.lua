require "base"
local msg = {}

-- s2c
local REQUEST = {}
msg.REQUEST = REQUEST

function REQUEST.notify(srvname,args)
end

function REQUEST.messagebox(srvname,args)
end

local RESPONSE = {}
msg.RESPONSE = RESPONSE
return msg
